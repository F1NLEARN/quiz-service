package com.finlearn.quizservice.quizsession.domain.event;

/**
 * 도메인 이벤트 마커 인터페이스.
 * 모든 도메인 이벤트는 이 인터페이스를 구현한다.
 * Spring, JPA 등 프레임워크에 의존하지 않는 순수 Java 인터페이스다.
 */
public interface DomainEvent {
}
